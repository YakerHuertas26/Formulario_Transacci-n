const formulario= document.getElementById('formulario');
const cantidad= formulario.querySelector('#cantidad');
const darForma=new Intl.NumberFormat('es-PE',{style:'currency',currency:'PEN'});

const fortmatoSoles= ()=>{
    
}; 
export default fortmatoSoles;